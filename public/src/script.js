const nome = document.getElementById('input-nome');
const matricula = document.getElementById('input-matricula');
const btnSubmit = document.getElementById('btn-submit-form-single');
const btnSubmitBatch = document.getElementById('btn-submit-form-batch');

btnSubmit.addEventListener('click', (e) => {
  e.preventDefault();

})

btnSubmitBatch.addEventListener('click', (e) => {
  e.preventDefault();

})